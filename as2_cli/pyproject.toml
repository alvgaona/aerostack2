[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.pixi.workspace]
name = "as2_cli"
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
as2_cli = { path = ".", editable = true }

[tool.pixi.tasks]

[tool.pixi.dependencies]
click = ">=8.3.1,<9"
